#!/usr/bin/env python
import os, sys, commands

qualifier=commands.getoutput('git describe')
pos=qualifier.find("-")
qualifier=qualifier[pos+1:]
input = sys.stdin
output = sys.stdout
for s in input:
	if s.startswith("qualifier="):
		s="qualifier=r" + qualifier + "\n"

	output.write(s)
